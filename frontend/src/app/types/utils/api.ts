const BASE_URL = "http://localhost:8080";   //izmeni ova dva
const API_BASE: string = `${BASE_URL}/api/types`; //tako da se u evirontment podesi

export const API_LIST: string = `${API_BASE}`;
export const API_ADD: string = `${API_BASE}`;
export const API_DELETE: string = `${API_BASE}/`;